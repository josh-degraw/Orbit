<UserControl x:Class="Orbit.Desktop.Components.ModuleComponentControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Orbit.Desktop.Components"
             xmlns:desktop="clr-namespace:Orbit.Desktop"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             >

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="{Binding ComponentName, FallbackValue=Component}"/>
        <TextBlock Grid.Row="1" Text="{Binding CurrentValue, FallbackValue=0.0}"/>
    </Grid>
</UserControl>
